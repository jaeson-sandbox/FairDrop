---
title: 'Phase 1 — FairDrop Wails Scaffold & Foundation'
type: 'chore'
created: '2026-08-22'
status: 'in-review' # draft | ready-for-dev | in-progress | in-review | done
review_loop_iteration: 0
baseline_commit: '20c211d5fe046ba66368ca1b421aa0c955632580'
context: ['{project-root}/docs/fairdrop-spec.md']
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** FairDrop is an empty repo holding only a spec. Phase 1 of the spec's execution plan (§10) requires a compiling Wails v2 + React/TS foundation with native OS file-drop enabled before any networking, streaming, or server work can begin.

**Approach:** Scaffold the `react-ts` Wails template in place at the repo root, configure `options.App` for native file drop and a standard OS window frame, declare the three `internal/` package contracts from spec §9 as compile-only interfaces, wire Tailwind v4, and prove the foundation by building a binary that receives real dropped absolute paths.

## Boundaries & Constraints

**Always:**
- Name everything **FairDrop**: Go module `fairdrop`, `outputfilename` `fairdrop`, window title `FairDrop`, mDNS service reserved as `_fairdrop._tcp`. The spec's "DeadDrop" is a stale working title.
- Scaffold **in place** at the repo root — never a nested `fairdrop/` subdirectory. Preserve existing `docs/`, `_bmad/`, `_bmad-output/`, `.git/`, `.gitignore`, `README.md`.
- Use `DragAndDrop: &options.DragAndDrop{EnableFileDrop: true}`. The spec's top-level `EnableFileDrop: true` does not exist in Wails v2.
- Frontend drop uses the runtime helper `OnFileDrop(cb, useDropTarget)`; the underlying event is `wails:file-drop`. The spec's `wails_file_drop` is wrong.
- `internal/` files in this phase declare types and interfaces ONLY — no method bodies beyond what compilation requires.

**Ask First:**
- Adding any dependency beyond Wails, Tailwind, and framer-motion.
- Any change to window chrome (frameless) — decision was standard OS frame.
- Committing or branching.

**Never:**
- Do not implement Phases 2–6: no IP resolution logic, no mDNS, no HTTP server, no zip/io.Pipe streaming, no progress tracking, no StageTransfer/CancelTransfer bodies.
- Do not build the DropZone/StagedView/TransferView UI beyond the minimal path-echo needed to prove file drop works.
- Do not hand-write `frontend/wailsjs/**` — it is generated.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Single file dropped on zone | User drags `C:\x\a.txt` onto the drop zone | `OnFileDrop` fires; UI renders the absolute path `C:\x\a.txt` | N/A |
| Directory dropped on zone | User drags folder `C:\x\dir` | UI renders the absolute directory path | N/A |
| Multi-file drop | User drags 3 files at once | All 3 absolute paths render | N/A |
| Drop outside the zone | Drop on window background (not the `--wails-drop-target` element) | Callback does not fire; UI unchanged | Silently ignored; webview must not navigate to the file |
| Component unmount | React drop component unmounts | `OnFileDropOff()` runs | No duplicate listeners on remount |

</frozen-after-approval>

## Code Map

Verified against a scratchpad probe of `wails init -n fairdrop -t react-ts` (CLI v2.15.0) and Wails source at `~/go/pkg/mod/github.com/wailsapp/wails/v2@v2.15.0`.

- `main.go` -- template entrypoint. Currently `Title: "fairdrop"`, no `DragAndDrop`, no `WindowStartState`. Primary edit target.
- `app.go` -- template `App` struct with `ctx context.Context`, `NewApp()`, `startup(ctx)`, and a `Greet` demo method to remove. `a.ctx` is mandatory for `runtime.EventsEmit` in later phases.
- `wails.json` -- generated with `name`/`outputfilename` = `fairdrop`, `frontend:dev:serverUrl: "auto"`. Author fields auto-filled from git config.
- `go.mod` -- `module fairdrop`, `go 1.25.0`, requires `wails/v2 v2.15.0`. Ships a trailing commented `// replace ...` line (template noise, safe to drop).
- `frontend/package.json` -- ships React **19.1**, Vite **7**, TypeScript 5.6, `@vitejs/plugin-react` 5.0. Spec §2 says React 18; the template is newer — use what ships.
- `frontend/vite.config.ts` -- minimal `plugins: [react()]`. Tailwind v4 plugin gets added here.
- `frontend/src/{App.tsx,main.tsx,style.css,App.css}` -- demo Greet UI to replace.
- `frontend/wailsjs/runtime/runtime.d.ts` -- **GOTCHA: the checked-in copy is stale and does NOT export `OnFileDrop`.** The complete definition lives at `wails/v2@v2.15.0/internal/frontend/runtime/wrapper/runtime.d.ts:239`. Regenerated by `wails build`/`wails dev`; TS type-check will fail until then.
- `wails/v2@v2.15.0/pkg/options/options.go:201-216` -- read-only evidence: `DragAndDrop{EnableFileDrop, DisableWebViewDrop, CSSDropProperty (default "--wails-drop-target"), CSSDropValue (default "drop")}`.
- `docs/fairdrop-spec.md` §9 -- source of the `NetworkManager`, `Streamer`, `TransferServer` contracts.

## Tasks & Acceptance

**Execution:**
- [x] Scaffold via `wails init` into a temp dir, then move generated files to repo root -- avoids the CLI's mandatory subdirectory while preserving existing repo contents.
- [x] `main.go` -- set `Title: "FairDrop"`, add `DragAndDrop: &options.DragAndDrop{EnableFileDrop: true}`, `WindowStartState: options.Normal`, `Frameless: false`, sensible `Width/Height` + `MinWidth/MinHeight` -- Phase 1 config per spec §10.
- [x] `app.go` -- remove `Greet`; keep `ctx` capture in `startup`; add `shutdown(ctx)` hook -- later phases need both lifecycle hooks for listener teardown.
- [x] `internal/network/network.go` -- declare `NetworkManager` interface -- spec §9 contract, no implementation.
- [x] `internal/stream/archiver.go` -- declare `Streamer` interface -- spec §9 contract, no implementation.
- [x] `internal/server/server.go` -- declare `TransferServer` interface plus the `TransferStats` struct it references -- spec §9 contract; `TransferStats` must exist or the interface will not compile.
- [x] `frontend` -- install `tailwindcss` + `@tailwindcss/vite` (v4: plugin in `vite.config.ts`, `@import "tailwindcss";` in `style.css`, NO `tailwind.config.js`, NO postcss config) and `framer-motion` -- spec §2 stack.
- [x] `frontend/src/App.tsx` -- replace demo UI with a minimal Tailwind drop zone carrying `--wails-drop-target: drop`, registering `OnFileDrop` and echoing received paths; `OnFileDropOff()` on unmount -- proves the foundation end-to-end.
- [x] `.gitignore` -- merge the template's Go/Node ignores with the existing file; ensure `frontend/node_modules/` and `build/bin/` are ignored -- the template writes its own `.gitignore` that would otherwise clobber the repo's.
- [x] `frontend/src/App.test.tsx` + `frontend/vite.config.ts` -- add `vitest`/`@testing-library/react`/`jsdom` (human-approved against the Ask First dependency gate) and cover every I/O matrix row -- the Matrix Test Audit requires each row to be covered by a test that ran.

**Acceptance Criteria:**
- Given a clean checkout, when `wails doctor` runs, then it reports the system ready for Wails development.
- Given the scaffolded root, when `go build ./...` runs, then it compiles with no errors across `main`, `internal/network`, `internal/stream`, `internal/server`.
- Given the frontend, when `npm run build` runs in `frontend/`, then `tsc` type-checks clean and Vite emits `frontend/dist/`.
- Given the full project, when `wails build` runs, then `build/bin/fairdrop.exe` is produced.
- Given the repo root after scaffolding, when listing files, then `docs/`, `_bmad/`, `_bmad-output/`, `README.md` and git history are all intact and no `fairdrop/` subdirectory exists.
- Given the interfaces, when inspected, then their method signatures match spec §9 exactly, except for the human-approved `context.Context` additions to `TransferServer.Start`, `Streamer.StreamFile`, and `Streamer.StreamZip` (see Spec Change Log).

## Spec Change Log

Deliberate divergences from `docs/fairdrop-spec.md`, verified against Wails v2.15.0. The API corrections are mirrored in a "Phase 1 Corrections" section at the top of the source spec so later phases do not reinstate them.

- **Wails file-drop option (spec §4, §10).** The spec calls for a top-level `EnableFileDrop: true` on `options.App`. No such field exists in Wails v2; the real form is `DragAndDrop: &options.DragAndDrop{EnableFileDrop: true}` (`pkg/options/options.go:201-216`).
- **Frontend drop event (spec §6 Module D).** The spec names a `wails_file_drop` event. The actual runtime event is `wails:file-drop`, and the supported API is the helper pair `OnFileDrop(callback, useDropTarget)` / `OnFileDropOff()` (`internal/frontend/runtime/desktop/draganddrop.js`).
- **mDNS service name (spec §6 Module A).** `_deaddrop._tcp` becomes `_fairdrop._tcp`, following the rename.
- **Product name (spec, throughout).** "DeadDrop" is a stale working title. Everything ships as **FairDrop**: Go module `fairdrop`, `outputfilename` `fairdrop`, window title `FairDrop`.
- **React version (spec §2).** The spec pins React 18; the `react-ts` template ships React 19.1 on Vite 7. Used what ships rather than downgrading a freshly generated template.
- **`context.Context` added to three §9 signatures (human-approved).** Spec §7 requires cancellation to "trigger a `context.CancelFunc` tied to the HTTP server to forcefully drop the connection", but the §9 signatures accept no ctx -- §7 and §9 contradict each other in the source document. Since these interfaces have no implementations yet, the contract is nearly free to correct now and expensive to correct in Phase 4, so `TransferServer.Start`, `Streamer.StreamFile`, and `Streamer.StreamZip` each take a leading `ctx context.Context`. `NetworkManager` is unchanged -- `StartBeacon`/`StopBeacon` are lifecycle calls and §9 stays authoritative there. This deliberately supersedes the "signatures match §9 exactly" acceptance criterion for those three methods.
- **Test dependencies (Ask First gate, human-approved).** `vitest`, `@testing-library/react`, and `jsdom` were added beyond the Wails/Tailwind/framer-motion allowance so the I/O matrix rows are covered by tests that actually run.

## Design Notes

Tailwind v4 differs sharply from the v3 setup most references still describe — no config file, no PostCSS, one CSS import:

```ts
// frontend/vite.config.ts
import tailwindcss from '@tailwindcss/vite'
export default defineConfig({ plugins: [tailwindcss(), react()] })
```

```css
/* frontend/src/style.css */
@import "tailwindcss";
```

Wails gates drops on a CSS custom property, not a class. The zone must compute `--wails-drop-target: drop`; Wails toggles `.wails-drop-target-active` on it during dragover for styling hooks.

## Verification

**Toolchain (required — neither binary is on this shell's default PATH):** Go 1.26.7 and Wails CLI v2.15.0 are installed, but the MSI updated the machine PATH after this session's environment was captured. Prefix every Go/Wails command with (bash syntax; `$HOME/go/bin` is the default `GOPATH/bin` -- confirm with `go env GOPATH`):
`export PATH="/c/Program Files/Go/bin:$HOME/go/bin:$PATH"`

**Commands:**
- `wails doctor` -- expected: "Your system is ready for Wails development!"
- `go build ./...` -- expected: exit 0, no output
- `go vet ./...` -- expected: exit 0
- `go test ./...` -- expected: exit 0; asserts the Wails options contract that no other check covers (`main_test.go`)
- `cd frontend && npm test` -- expected: 9 passed, covering all 5 matrix rows
- `cd frontend && npm run build` -- expected: tsc clean, `dist/` emitted
- `wails build` -- expected: exit 0, `build/bin/fairdrop.exe` exists

**Matrix coverage note:** the native half of a drop (OS -> webview -> absolute paths) is Wails runtime code and cannot execute under jsdom. `App.test.tsx` mocks `../wailsjs/runtime/runtime`, then invokes the exact callback the app registered — so it verifies our contract with Wails and what the app does with the paths handed to it. Row 4 ("drop outside the zone") is exercised rather than assumed: the test mirrors `checkStyleDropTarget()` from `draganddrop.js`, resolving `--wails-drop-target` up the ancestor chain the way inheritance would, and asserts that a drop aimed at the heading never reaches the app while an already-rendered path stays put. Suite validated by mutation: setting the custom property to a non-matching value fails 7 of the 9 tests, and flipping `EnableFileDrop` to `false` in `main.go` fails `go test ./...` while leaving `go build`, `go vet`, `npm test`, `npm run build`, and `wails build` all green.

**Manual checks (if no CLI):**
- Run `build/bin/fairdrop.exe`: window titled "FairDrop" opens with native Windows chrome and a visible drop zone. Dragging a file onto the zone renders its absolute path; dragging onto the background does nothing and does not navigate the webview.
